# Testcontainers configuration for Podman in WSL2
# Only use TCP strategy to avoid Unix socket connection attempts
docker.client.strategy=org.testcontainers.dockerclient.EnvironmentAndSystemPropertyClientProviderStrategy
ryuk.disabled=true
